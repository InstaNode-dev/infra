---
apiVersion: v1
kind: Service
metadata:
  name: instant-migrator
  namespace: instant-infra
spec:
  type: NodePort
  selector:
    app: instant-migrator
  ports:
    - name: http
      port: 8090
      targetPort: 8090
      nodePort: 30090
